gazetools 3.1
=============
  
* pva no longer requires a time parameter
* all classification algorithms now take a parameter called blinks which is a vector indicating which samples belong to a blink
* added a new classification algorithm that uses a mixture model
* added a new function that returns a scanpath given a set of fixations and list of ROIs

gazetools 4.0
=============

* fix import conflicts that resulted in previous version getting archived
* documentation and example improvements